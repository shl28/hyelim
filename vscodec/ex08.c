#include <stdio.h>
int main(){
    int a = 10, b = 3;

    printf("a > b: %d\n", a > b);
    printf("a < b: %d\n", a < b);
    printf("a == b: %d\n", a == b);
    printf("a != b: %d\n", a != b);
    // true = 1 // false = 0 // %d 로 출력하기에 숫자로 나옴

    return 0;
}